#!/bin/bash
#
# Aim:
#    This code will add the directory of this toolkit into the PATH system variable. 
# 

source $(dirname "${BASH_SOURCE[0]}")/Pipeline/bin_setup.bash
source $(dirname "${BASH_SOURCE[0]}")/Software/bin_setup.bash






